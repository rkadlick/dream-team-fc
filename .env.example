# Supabase project URL, e.g. https://abcdefghijklm.supabase.co
NEXT_PUBLIC_SUPABASE_URL=

# The anon / publishable key. This is the ONLY key the app uses — there is no
# service-role key anywhere, so RLS is the real enforcement.
NEXT_PUBLIC_SUPABASE_ANON_KEY=
